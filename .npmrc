# Allow legacy peer deps to avoid Hardhat dependency conflicts
# This is needed because Netlify auto-installs root package.json (Hardhat deps)
# before running our build command. We only actually need frontend dependencies,
# but this allows the root install to proceed without errors.
legacy-peer-deps=true

